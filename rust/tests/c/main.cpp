#include <iostream>

using namespace std;

// Function template to find the maximum of two values
template <typename T>
T max(T a, T b) {
  return (a > b) ? a : b;
}

int main() {
  // int int1 = 10, int2 = 20;
  // double double1 = 10.5, double2 = 20.5;
  // cout << "Max of int: " << max(int1, int2) << "\n";
  // cout << "Max of double: " << max(double1, double2) << "\n";

  // return 0;
}